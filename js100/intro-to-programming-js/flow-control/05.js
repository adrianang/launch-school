// Launch School, Introduction to Programming with JavaScript
// Introduction to Programming / Flow Control / Exercise 05

/**
 * 05. Refactor this statement to use an if statement instead.
 *     
 *     return foo() ? 'bar' : qux();
 */

if (foo()) {
  return 'bar';
} else {
  return qux();
}